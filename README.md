# auskinimmanuel.github.io

Source for my personal site: https://auskinimmanuel.github.io

Built with [Astro](https://astro.build). All content lives in `src/data/site.ts`.
Pushing to `main` builds and deploys via GitHub Actions.

```
npm install
npm run dev      # local preview
npm run build    # production build to dist/
```
